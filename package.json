{
  "name": "schemish",
  "version": "0.0.1",
  "description": "Experimental parser/generator for an experimental language. Only for learning and toying around.",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc",
    "test": "mocha --recursive --require ts-node/register test/**/*.spec.ts"
  },
  "author": "Matt Machuga",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.3",
    "@types/mocha": "^5.2.7",
    "@types/node": "^16.11.7",
    "chai": "^4.2.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
